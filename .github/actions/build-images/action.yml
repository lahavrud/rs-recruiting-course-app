name: Build & push images
description: >-
  Build the shared base image then the api + worker images and push them to
  the ops-account ECR. The base is pushed first so the per-service buildx
  builds resolve it as their FROM from the registry (sharing a locally-built
  base across separate buildx invocations is unreliable). Assumes the caller
  has already logged in to the registry (aws-actions/amazon-ecr-login).

inputs:
  registry:
    description: "ECR registry host, e.g. <ops-account>.dkr.ecr.eu-central-1.amazonaws.com"
    required: true
  image-tag:
    description: "Tag for the api + worker images (and the base image)"
    required: true

runs:
  using: composite
  steps:
    - uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0

    - name: Build & push shared base
      uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
      with:
        context: .
        file: docker/base.Dockerfile
        push: true
        provenance: false
        sbom: false
        tags: ${{ inputs.registry }}/rs-recruiting-course/base:${{ inputs.image-tag }}
        cache-from: type=gha,scope=base
        cache-to: type=gha,mode=max,scope=base

    - name: Build & push api
      uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
      with:
        context: .
        file: services/api/Dockerfile
        build-args: |
          BASE_IMAGE=${{ inputs.registry }}/rs-recruiting-course/base:${{ inputs.image-tag }}
        push: true
        provenance: false
        sbom: false
        tags: ${{ inputs.registry }}/rs-recruiting-course/api:${{ inputs.image-tag }}
        cache-from: type=gha,scope=api
        cache-to: type=gha,mode=max,scope=api

    - name: Build & push worker
      uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
      with:
        context: .
        file: services/worker/Dockerfile
        build-args: |
          BASE_IMAGE=${{ inputs.registry }}/rs-recruiting-course/base:${{ inputs.image-tag }}
        push: true
        provenance: false
        sbom: false
        tags: ${{ inputs.registry }}/rs-recruiting-course/worker:${{ inputs.image-tag }}
        cache-from: type=gha,scope=worker
        cache-to: type=gha,mode=max,scope=worker
